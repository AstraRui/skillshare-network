target-version = "py312"
line-length = 100

extend-exclude = [
  "alembic/versions",
]

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501", "B008"]

[lint.isort]
known-first-party = ["app"]

