# Локальная разработка: прокси API на бэкенд
# Без Docker: бэкенд на localhost:8000 — значение по умолчанию в vite.config.js
# В Docker Compose для сервиса frontend задано VITE_PROXY_TARGET=http://backend:8000

VITE_PROXY_TARGET=http://localhost:8000
